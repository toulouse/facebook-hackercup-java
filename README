To run, 'mvn clean compile' followed by 'mvn exec:java'. It will pick up the input file located in the appropriate directory within src/main/resources/se/atoulou/facebook/hackercup/.
It will output to output.txt. If you would prefer to process stdin/stdout instead, modify the 'in' and 'out' variables in HackRunner.java to System.in/System.out, respectively.
